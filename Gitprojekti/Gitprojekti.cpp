#include <iostream>
using namespace std;

int main()
{
    int a, b, toiminto;
    cout << "Anna ensimmäinen luku: " << endl;
    cin >> a;
    cout << "Anna toinen luku: " << endl;
    cin >> b;
    cout << "Valitse toiminto: " << endl;
    cin >> toiminto;

    if (toiminto == 1) {
        //plus
    }
    else if (toiminto == 2) {
        //miinus
    }
    else if (toiminto == 3) {
        //kerto
    }
    else {
        //jako
    }
}

